package ex20221118

fun main() {

    // 연산자, 제어문(if, when)

    // 항, 연산자 -> 식

    // 1) 산술 연산자 : +, -, *, /, %
    // 2) 대입 연산자(복합대입연산자) : =, +=, -=, /=, *=, %=
    // 3) 증감 연산자 : ++, -- (전치, 후치)
    // 4) 비교 연산자 : <, >, >= ... ===, !==
    // 5) 논리 연산자 : &&, ||, !

    // when ** (조건문)


}